spring:
  datasource:
    url: jdbc:h2:mem:whatsapp_test;MODE=PostgreSQL;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  liquibase:
    change-log: classpath:db/changelog/db.changelog-master.yaml

whatsapp:
  access-token: test-token
  phone-number-id: test-phone-number-id
  business-account-id: test-waba
  verify-token: AltitudeLabs@2026
  template-name: altitude_welcome_promo
  template-language: en
  api-version: v23.0
  auto-reply-message: Thank you for contacting us.
  webhook:
    verify-token: AltitudeLabs@2026
