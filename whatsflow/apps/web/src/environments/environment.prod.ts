export const environment = {
  production: true,
  // Same-origin /api — set Vercel rewrite to your hosted Spring API, or change this URL before deploy.
  apiBaseUrl: '/api',
  appName: 'WhatsFlow',
};
